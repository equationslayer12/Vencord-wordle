import { classNameFactory } from "@api/Styles";


// used to generate class names for css,
// so we don't have to write the prefix vc-wordle- every time
export const cl = classNameFactory("vc-wordle-");
